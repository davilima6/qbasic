  CLS
  SCREEN 9, 9
  PALETTE 1, 0
  COLOR 14, 1
  
   DATA "COM CERTEZA","E DA°?","ERA PRA RIR?"
   DATA "CONCORDO","SIM","NéO","CLARO!","
   DATA "OK", "CERTO!", "VOCà QUE SABE..."
   DATA "ê MESMO?!","REPITA O QUE DISSE"
   DATA "QUE INTERESSANTE!","VOCà ACHA?"
   DATA "ISSO PREOCUPA VOCà?","AH....","ISSO ê DA SUA CONTA?!!!?"
   DIM R$(14), R(14)
  
   LET W = W + 1
   FOR I = 1 TO 14
   READ R$(I)
    LET R(I) = 0
    NEXT I
   
    CLS
    PRINT TAB(INT(W / 2 - 6)); "<<DOUTOR BESTEIRA>>"
    PRINT
150 PRINT "DIGA ALGUM FATO INTERESSANTE"
160 PRINT
    LINE INPUT F$
    LET F$ = UCASE$(F$)

    IF F$ = "THAU" THEN GOTO 380
    IF F$ = "ADEUS" THEN GOTO 380
    IF F$ = "" THEN GOTO 150
    IF F$ = "NAO" THEN GOTO 340
    IF F$ = "SIM" THEN GOTO 360
   
    RANDOMIZE TIMER
210 LET R = INT(RND * 14) + 1
    IF W = 14 THEN GOTO 280
    IF R(R) = 1 THEN GOTO 210
    LET R(R) = 1
    PRINT R$(R)
    LET W = W + 1
    GOTO 160
280 FOR I = 1 TO 14
    LET R(I) = 0
    NEXT I
    PRINT "POR QUE VOCà DIZ QUE "; F$; "?"
    LET W = 0
    GOTO 160
340 PRINT "POR QUE NéO?"
    GOTO 160
360 PRINT "TEM CERTEZA?"
    GOTO 160

380 CLS
    LOCATE 5, 15: PRINT "SEU SEM-VERGONHA! NUNCA MAIS ME USE!"

